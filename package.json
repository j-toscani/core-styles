{
  "name": "jt-core-style",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "sass --watch src/input.scss build/style.css",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.66.1"
  }
}
