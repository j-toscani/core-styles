{
  "name": "jt-core-style",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "sass --watch src/input.scss example/style.css",
    "serve": "bun run ./example/serve.ts",
    "build": "sass src/input.scss build/style.min.css --style compressed"
  },
  "devDependencies": {
    "hono": "^4.4.0",
    "sass": "^1.66.1"
  }
}
