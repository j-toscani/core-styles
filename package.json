{
  "name": "jt-core-style",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "sass --watch src/input.scss build/style.css",
    "build": "sass src/input.scss build/style.min.css --style compressed",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.66.1"
  }
}
